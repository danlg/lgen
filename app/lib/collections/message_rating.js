MessageRating = new Mongo.Collection('MessageRating');


if (Meteor.isServer) {
  MessageRating.allow({
    insert: function (userId, doc) {
      return false;
    },

    update: function (userId, doc, fieldNames, modifier) {
      return false;
    },

    remove: function (userId, doc) {
      return false;
    }
  });
}
